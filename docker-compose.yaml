version: "3.8"
services:
  
  web-app:
    build: ./web-app
    ports:
      - "5000:5000"
    depends_on:
      - db
    command: npm start
  
  db:
    image: mongo:latest
    ports: 
      - "27017:27017"
    
